# Proxmox / on-premise deployment with Aspire Dashboard
# Usage:
#   docker compose -f docker-compose.proxmox.yml up -d
#
# Prerequisites:
#   mkdir -p /opt/hgvmate/data
#
# Dashboard UI: http://<proxmox-ip>:18888
# MCP endpoint:  http://<proxmox-ip>:5000/mcp

services:
  hgvmate:
    image: ghcr.io/roysalisbury/hgvmate:latest
    container_name: hgvmate-mcp
    environment:
      - HGVMATE_TRANSPORT=sse
      # Relative path = resolves under /data → persistent NVMe storage
      # Clones survive container restarts/upgrades (no re-clone needed)
      - RepoSync__ClonePath=repos
      - RepoSync__MinFreeDiskSpaceMb=2048
      # ONNX thread control — 0=auto-detect (half of available CPUs)
      - Search__OnnxThreadCount=0
      - Search__OnnxBatchSize=32
      # Telemetry → Aspire Dashboard sidecar
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://aspire-dashboard:18889
      # Credentials (set these or use a .env file)
      # - GITHUB_TOKEN=
      # - AZURE_DEVOPS_PAT=
    env_file: .env
    ports:
      - "5000:5000"
    volumes:
      - /opt/hgvmate/data:/data
    deploy:
      resources:
        limits:
          cpus: "8.0"
          memory: 6g
        reservations:
          cpus: "2.0"
          memory: 1g
    depends_on:
      - aspire-dashboard
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped

  aspire-dashboard:
    image: mcr.microsoft.com/dotnet/aspire-dashboard:latest
    container_name: aspire-dashboard
    environment:
      # Restrict dashboard to localhost only; set to true and remove ports binding
      # for production, or configure authentication via DOTNET_DASHBOARD_FRONTEND_AUTHMODE
      - DOTNET_DASHBOARD_UNSECURED_ALLOW_ANONYMOUS=true
    ports:
      # Bind to localhost only — not exposed to the network
      - "127.0.0.1:18888:18888"
    restart: unless-stopped
